import { Container } from 'react-bootstrap';

const Items = () => {
    return (
        <Container className="my-5">
            <h1>Items/Products</h1>
            <p>This is the Items/Products (lost or found reports) page.</p>
        </Container>
    );
};

export default Items;
